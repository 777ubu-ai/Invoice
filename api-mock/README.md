# Sprint 2 API — Mock

Mock HTTP API that imitates the Sprint 2 endpoints (see TZ section 7), so the bot can run
end-to-end before the real Sprint 2 is implemented.

Endpoints to implement (next step):

- `POST /api/invoices/upload`
- `POST /api/invoices/:id/classify`
- `GET  /api/invoices/:id`
- `PATCH /api/invoices/:id/items/:i`
- `POST /api/invoices/:id/approve`
- `GET  /api/clients`
- `GET  /api/tnved/search`

Returns canned data based on the demo scenarios in TZ sections 4.2–4.4.
